import { redirect } from "next/navigation";
import { getAdminUser } from "@/lib/admin-auth";
export const dynamic="force-dynamic";
export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string;reset?:string}>}){if(await getAdminUser())redirect("/admin");const {error,reset}=await searchParams;return <main className="grid min-h-screen place-items-center bg-[#f4f8f6] p-5"><form method="post" action="/api/auth/login" className="w-full max-w-md rounded-2xl border bg-white p-7 shadow-sm"><p className="text-xs font-bold tracking-[.18em] text-emerald-700">EGE TEKNİK</p><h1 className="mt-2 text-2xl font-bold">Güvenli yönetici girişi</h1><p className="mt-2 text-sm text-zinc-600">Yetkili e-posta ve parolanızla giriş yapın.</p>{reset&&<p role="status" className="mt-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800">Parolanız güncellendi. Yeni parolanızla giriş yapabilirsiniz.</p>}{error&&<p role="alert" className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">Giriş bilgileri doğrulanamadı.</p>}<label className="mt-6 block text-sm font-semibold">E-posta<input name="email" type="email" autoComplete="username" required maxLength={254} className="mt-1 h-11 w-full rounded-lg border px-3"/></label><label className="mt-4 block text-sm font-semibold">Parola<input name="password" type="password" autoComplete="current-password" required minLength={12} maxLength={200} className="mt-1 h-11 w-full rounded-lg border px-3"/></label><button className="mt-6 h-11 w-full rounded-lg bg-[#07261d] font-semibold text-white">Giriş yap</button><p className="mt-4 text-center text-sm"><a href="/admin/forgot-password" className="font-semibold text-emerald-700 underline underline-offset-2">Şifremi unuttum</a></p></form></main>}
