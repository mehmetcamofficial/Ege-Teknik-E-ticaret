/* Lists the currently effective legal documents from the database-backed API (single source of truth). */
(async()=>{const list=document.querySelector('[data-legal-index]');if(!list)return;const esc=v=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));try{const r=await fetch('/api/legal/documents');const d=await r.json();if(!r.ok||!Array.isArray(d.documents)||!d.documents.length)throw new Error('empty');list.innerHTML=d.documents.map(x=>`<li><a href="/legal/${encodeURIComponent(x.slug)}">${esc(x.title)}</a></li>`).join('')}catch{list.innerHTML='<li>Yasal metinler şu anda yayında değil.</li>'}})();
