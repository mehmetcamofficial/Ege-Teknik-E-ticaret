/* eslint-disable @typescript-eslint/no-unused-vars -- called from the delegated data-action handler, page scripts and tests */
if(!document.querySelector('link[href="phase1b.css"]'))document.head.insertAdjacentHTML('beforeend','<link rel="stylesheet" href="phase1b.css"><link rel="stylesheet" href="phase1c.css">');
const money=n=>new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY',maximumFractionDigits:0}).format(n);
/* Every value that is not a literal in this file (API data, admin-authored content, URL
   parameters) goes through esc() before it is interpolated into an innerHTML template,
   as text or as an attribute value. Nothing rendered here is intended to be rich HTML. */
const esc=s=>String(s??'').replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/'/g,'&#39;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
const productImageMarkup=(p,cls,loading)=>{const src=safeImageSrc(p.imageUrl);return src?`<img class="${cls}" src="${esc(src)}" alt="${esc(p.name)}"${loading?` loading="${loading}"`:''}>`:''};
/* Navigation driven by a data-* value is only followed when it resolves to this site: a
   relative/same-origin http(s) URL. javascript:, data:, vbscript:, //other-host and absolute
   external URLs resolve to a different origin or scheme and return null. */
function safeInternalHref(href){try{const url=new URL(String(href??''),location.href);if(url.origin!==location.origin||!/^https?:$/.test(url.protocol))return null;return url.pathname+url.search+url.hash}catch{return null}}
/* The catalog shown anywhere on the storefront (homepage, catalog, product, favorites,
   compare, checkout) is only ever what GET /api/products returned - there is no bundled
   fallback price list. Until it answers the UI says so ('loading'); if it fails it says
   that ('unavailable'). Orders are refused unless the state is 'ready', and the server
   re-prices every order line regardless of what the page displayed. */
let catalogProducts=[],catalogState='loading';
const getProducts=()=>catalogProducts;
function catalogAuthoritative(){return catalogState==='ready'}
function catalogNotice(){return catalogState==='loading'?'Ürün bilgileri yükleniyor…':'Ürün bilgileri şu anda alınamıyor. Lütfen daha sonra tekrar deneyin.'}
function renderProductViews(){renderCatalog();renderProductPage();renderFavorites();renderCompare();renderCheckout();renderFeaturedProducts();updateCartCount()}
async function loadCatalog(){try{const response=await fetch('/api/products');if(!response.ok)throw new Error(`catalog ${response.status}`);const data=await response.json();catalogProducts=(Array.isArray(data.products)?data.products:[]).map(p=>({...p,energy:p.energyClass,sale:p.saleMode==='online'}));catalogState='ready';pruneCart()}catch{catalogState='unavailable';console.info('Ürün kataloğu alınamadı.')}renderProductViews()}

/* Cart state is only {productId, quantity}. Price, VAT, stock and totals always come from
   the catalog the server served, never from localStorage. */
const CART_MAX_QUANTITY=10,CART_MAX_LINES=20,PRODUCT_ID_PATTERN=/^[A-Za-z0-9._:-]{1,160}$/;
function normalizeCartEntries(raw,knownIds){
  if(!Array.isArray(raw))return [];
  const merged=new Map();
  for(const entry of raw){
    if(!entry||typeof entry!=='object')continue;
    const productId=typeof entry.productId==='string'&&entry.productId?entry.productId:(typeof entry.id==='string'?entry.id:'');
    if(!PRODUCT_ID_PATTERN.test(productId))continue;
    if(knownIds&&!knownIds.has(productId))continue;
    const parsed=Number(entry.quantity??entry.qty??1);
    const quantity=Number.isFinite(parsed)&&parsed>=1?Math.min(Math.floor(parsed),CART_MAX_QUANTITY):1;
    merged.set(productId,Math.min((merged.get(productId)||0)+quantity,CART_MAX_QUANTITY));
  }
  return [...merged].slice(0,CART_MAX_LINES).map(([productId,quantity])=>({productId,quantity}));
}
function cartLines(entries,products){
  const index=new Map((products||[]).map(p=>[p.id,p]));
  return (entries||[]).map(entry=>{const product=index.get(entry.productId)||null;return {productId:entry.productId,quantity:entry.quantity,product,available:Boolean(product&&product.sale)}});
}
function cartTotal(lines){return (lines||[]).reduce((sum,line)=>line.available?sum+line.product.price*line.quantity:sum,0)}
function orderItemsPayload(entries,products){return cartLines(entries,products).filter(line=>line.available).map(line=>({productId:line.productId,quantity:line.quantity}))}
function buildOrderPayload(fields,entries,products){return {customerName:fields.customerName||'',phone:fields.phone||'',email:fields.email||'',city:fields.city||'',address:fields.address||'',paymentProvider:fields.paymentProvider||'discovery',items:orderItemsPayload(entries,products)}}
function orderAttemptKey(store){let key=null;try{key=store.getItem('ege-order-attempt')}catch{}if(!key){key=crypto.randomUUID();try{store.setItem('ege-order-attempt',key)}catch{}}return key}
function clearOrderAttemptKey(store){try{store.removeItem('ege-order-attempt')}catch{}}
function readCartRaw(){try{return JSON.parse(localStorage.getItem('ege-cart')||'[]')}catch{return []}}
function knownProductIds(){return catalogAuthoritative()?new Set(getProducts().map(p=>p.id)):null}
function writeCart(entries){localStorage.setItem('ege-cart',JSON.stringify(normalizeCartEntries(entries,null)));updateCartCount()}
/* Drops cart lines whose ids are not in the served catalog, so legacy ids such as
   "aphro-09" disappear instead of failing at checkout. */
function pruneCart(){if(!catalogAuthoritative())return 0;const before=normalizeCartEntries(readCartRaw(),null),after=normalizeCartEntries(before,knownProductIds());if(after.length!==before.length)writeCart(after);return before.length-after.length}
const getCart=()=>normalizeCartEntries(readCartRaw(),knownProductIds());
const saveCart=c=>writeCart(c);
const getFavorites=()=>JSON.parse(localStorage.getItem('ege-favorites')||'[]');
const getCompare=()=>JSON.parse(localStorage.getItem('ege-compare')||'[]');
function toggleFavorite(id){let ids=getFavorites();ids=ids.includes(id)?ids.filter(x=>x!==id):[...ids,id];localStorage.setItem('ege-favorites',JSON.stringify(ids));toast(ids.includes(id)?'Favorilere eklendi':'Favorilerden çıkarıldı');renderFavorites();updateFavoritesCount()}
function toggleCompare(id){let ids=getCompare();if(ids.includes(id))ids=ids.filter(x=>x!==id);else if(ids.length<4)ids.push(id);else return toast('En fazla 4 ürün karşılaştırılabilir');localStorage.setItem('ege-compare',JSON.stringify(ids));toast(ids.includes(id)?'Karşılaştırmaya eklendi':'Karşılaştırmadan çıkarıldı');renderCompare()}
function updateCartCount(){const count=getCart().reduce((sum,entry)=>sum+entry.quantity,0);document.querySelectorAll('[data-cart-count]').forEach(x=>x.textContent=count)}
function updateFavoritesCount(){const count=getFavorites().length;document.querySelectorAll('[data-favorites-count]').forEach(x=>x.textContent=count)}
function toast(t){const e=document.querySelector('.toast');if(!e)return;e.textContent=t;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),2400)}
function addCart(id){const p=getProducts().find(x=>x.id===id);if(!p||!p.sale)return;writeCart([...readCartRaw(),{productId:id,quantity:1}]);renderCheckout();toast('Ürün sepete eklendi')}
function quote(id){const p=getProducts().find(x=>x.id===id);location.href='https://wa.me/905427957560?text='+encodeURIComponent((p?.name||'GREE ürün')+' için fiyat ve keşif bilgisi almak istiyorum. Sayfa: '+location.href)}
/* Only the specs the catalog actually carries are shown - an empty wifi/energy value renders nothing, not a blank label. */
function specChips(p){return [p.capacity,p.energy,p.wifi&&`Wi-Fi: ${p.wifi}`].filter(Boolean).map(v=>`<span class="spec">${esc(v)}</span>`).join('')}
function productCard(p){const fav=getFavorites().includes(p.id),cmp=getCompare().includes(p.id),id=esc(p.id),href=`product.html?id=${encodeURIComponent(p.id)}`;return `<article class="product"><a href="${href}" class="product-visual"><span class="badge ${p.sale?'':'quote'}">${p.sale?'ONLINE SATIŞ':'TEKLİF / KEŞİF'}</span>${productImageMarkup(p,'product-image','lazy')||'<div class="unit"></div>'}</a><div class="card-tools"><button type="button" title="Favori" data-action="toggle-favorite" data-id="${id}">${fav?'♥':'♡'}</button><button type="button" data-action="toggle-compare" data-id="${id}">${cmp?'Karşılaştırmada':'Karşılaştır'}</button></div><h3><a href="${href}">${esc(p.name)}</a></h3><div class="meta">${esc([p.category,p.series].filter(Boolean).join(' · '))}</div><div class="specs">${specChips(p)}</div><div class="product-foot"><div class="price">${p.sale?money(p.price):'Fiyat Sor'}<small>${p.sale?(p.stock>0?`Stok: ${esc(p.stock)}`:'Stokta yok'):'Projelendirme ile satılır'}</small></div><div class="actions"><a class="ghost" href="${href}">İncele</a><button type="button" class="primary" data-action="${p.sale?'add-cart':'quote'}" data-id="${id}">${p.sale?'Sepete Ekle':'Teklif'}</button></div></div></article>`}
/* Real capacities are written "12000 BTU/h"; the chips say "12.000". Compare digits only, and only for BTU-rated products. */
const capacityBtuDigits=p=>/btu/i.test(p.capacity||'')?p.capacity.replace(/\D/g,''):'';
function renderCatalog(){const root=document.querySelector('[data-products]');if(!root)return;const count=document.querySelector('[data-result-count]');if(!catalogAuthoritative()){root.innerHTML=`<div class="empty">${catalogNotice()}</div>`;count?.replaceChildren();return}const cat=document.querySelector('[name=category]:checked')?.value||'Tümü',q=(document.querySelector('#catalog-search')?.value||'').toLocaleLowerCase('tr'),btu=document.querySelector('.chip.active')?.dataset.btu||'Tümü';const ps=getProducts().filter(p=>(cat==='Tümü'||p.category===cat)&&(btu==='Tümü'||capacityBtuDigits(p)===btu.replace(/\D/g,''))&&(!q||[p.name,p.series,p.category].join(' ').toLocaleLowerCase('tr').includes(q)));root.innerHTML=ps.length?ps.map(productCard).join(''):'<div class="empty">Bu filtrelerle eşleşen ürün bulunamadı.</div>';count?.replaceChildren(document.createTextNode(ps.length+' ürün'))}
function renderCheckout(){const root=document.querySelector('[data-cart-items]');if(!root)return;const subtotalEl=document.querySelector('[data-subtotal]'),totalEl=document.querySelector('[data-total]'),setTotals=t=>{if(subtotalEl)subtotalEl.textContent=t;if(totalEl)totalEl.textContent=t};
  // Never show a price or an "unavailable" verdict for a line before the server catalog has answered.
  if(!catalogAuthoritative()){root.innerHTML=getCart().length?`<p>${catalogNotice()}</p>`:'<p>Sepetiniz boş.</p>';setTotals('—');return}
  const lines=cartLines(getCart(),getProducts()),sellable=lines.filter(l=>l.available),blocked=lines.filter(l=>!l.available);
  /* type="button" is required, not cosmetic: these rows render inside checkout.html's
     <form data-checkout-form>, and a <button> with no type defaults to submit - so a
     remove click would also fire submitOrder and place an order for the rest of the cart. */
  const rows=[...sellable.map(l=>`<div class="summary-row"><span>${esc(l.product.name)}<br><small>${esc(l.product.capacity)} · ${l.quantity} adet</small></span><span>${money(l.product.price*l.quantity)} <button type="button" class="ghost" data-action="remove-cart" data-id="${esc(l.productId)}">×</button></span></div>`),
    ...blocked.map(l=>`<div class="summary-row"><span>Bu ürün artık satışta değil<br><small>Ürün kodu: ${esc(l.productId)}</small></span><span><button type="button" class="ghost" data-action="remove-cart" data-id="${esc(l.productId)}">×</button></span></div>`)];
  root.innerHTML=rows.length?rows.join(''):'<p>Sepetiniz boş.</p>';
  setTotals(money(cartTotal(lines)))}
function removeCart(productId){writeCart(readCartRaw().filter(entry=>(entry?.productId??entry?.id)!==productId));renderCheckout()}
async function submitOrder(e){e.preventDefault();const f=e.currentTarget,button=f.querySelector('button.primary'),result=f.querySelector('[data-order-result]'),say=t=>{if(result)result.textContent=t};
  if(!catalogAuthoritative()){say('Ürün bilgileri sunucudan doğrulanamadı. Lütfen sayfayı yenileyip tekrar deneyin.');return}
  const entries=getCart(),products=getProducts(),payload=buildOrderPayload({customerName:'',phone:'',email:'',city:'',address:'',paymentProvider:''},entries,products);
  if(!payload.items.length){toast('Sepetiniz boş');say('Sepetinizde satın alınabilir ürün yok.');return}
  const d=new FormData(f);Object.assign(payload,{customerName:d.get('customerName'),phone:d.get('phone'),email:d.get('email'),city:d.get('city'),address:d.get('address'),paymentProvider:d.get('provider')});
  const attemptKey=orderAttemptKey(sessionStorage);
  if(button){button.disabled=true;button.textContent='Sipariş kaydediliyor…'}
  try{
    const response=await fetch('/api/orders',{method:'POST',headers:{'content-type':'application/json','idempotency-key':attemptKey},body:JSON.stringify(payload)});
    const data=await response.json().catch(()=>({}));
    if(!response.ok)throw new Error(data.error||'Sipariş kaydedilemedi');
    // Cart and attempt key are cleared only once the server has confirmed the order.
    clearOrderAttemptKey(sessionStorage);writeCart([]);renderCheckout();
    if(result)result.innerHTML=`<b>Siparişiniz kaydedildi.</b><br>Takip numarası: ${esc(data.orderNumber)}<br>Ege Teknik ekibi ödeme ve montaj için sizinle iletişime geçecek.`;
    if(button)button.textContent='Sipariş Kaydedildi'
  }catch(error){
    if(button){button.disabled=false;button.textContent='Tekrar Dene'}
    say(error.message||'Sipariş kaydedilemedi. Bağlantınızı kontrol edip tekrar deneyin.')
  }}

/**
 * Single delegated click handler for every dynamically-rendered and static
 * data-action="..." control across the storefront (product cards, cart/compare
 * remove buttons, the header's mobile menu toggle, etc.) - registered once,
 * here, instead of onclick="..." attributes in the generated HTML. The site's
 * CSP has no 'unsafe-hashes', so inline event-handler attributes are silently
 * blocked; addEventListener is a normal DOM API call and is unaffected. This
 * also means re-rendered markup (a new catalog filter, a new cart line) never
 * needs its listeners rebound - the listener lives on `document`, not on the
 * elements it acts on.
 *
 * data-* attributes here only ever carry plain identifiers or URLs (never
 * executable script) - the handler is what decides what to do with them.
 */
function handleDelegatedClick(e){
  const el=e.target.closest('[data-action]');
  if(!el)return;
  const action=el.dataset.action,id=el.dataset.id;
  if(action==='add-cart')addCart(id);
  else if(action==='quote')quote(id);
  else if(action==='toggle-favorite')toggleFavorite(id);
  else if(action==='toggle-compare')toggleCompare(id);
  else if(action==='remove-cart')removeCart(id);
  else if(action==='toggle-menu')document.querySelector('.nav-links')?.classList.toggle('open');
  else if(action==='calculate-btu')calculateBtu();
  else if(action==='navigate'){const to=safeInternalHref(el.dataset.href);if(to)location.href=to}
}
document.addEventListener('click',handleDelegatedClick);

document.addEventListener('DOMContentLoaded',()=>{updateCartCount();updateFavoritesCount();renderCatalog();renderCheckout();document.querySelector('[data-checkout-form]')?.addEventListener('submit',submitOrder);document.querySelectorAll('[name=category]').forEach(x=>x.addEventListener('change',renderCatalog));document.querySelector('#catalog-search')?.addEventListener('input',renderCatalog);document.querySelectorAll('.chip').forEach(x=>x.addEventListener('click',()=>{document.querySelectorAll('.chip').forEach(y=>y.classList.remove('active'));x.classList.add('active');renderCatalog()}));void loadCatalog()});

const business={name:'Ege Teknik',phone:'0542 795 75 60',phoneHref:'tel:+905427957560',wa:'https://wa.me/905427957560',address:'İkiçeşmelik Mahallesi Süleyman Demirel Bulvarı, Ege Uluçınar Koop. No:13/1D, 09400 Kuşadası/Aydın',map:'https://share.google/YHInB4tNwB2khqC10'};
function renderHeader(){const root=document.querySelector('[data-site-header]');if(!root)return;root.innerHTML=`<header class="store-header"><div class="store-top"><span>Kuşadası merkezli • Tüm Ege Bölgesi</span><a href="${business.phoneHref}">${business.phone}</a></div><nav class="store-nav"><a class="brand" href="index.html">EGE TEKNİK<small>KLİMA & TEKNOLOJİ</small></a><button class="menu-toggle ghost" data-action="toggle-menu">Menü</button><div class="nav-links"><a href="catalog.html">GREE Klimalar</a><a href="services.html">Hizmetler</a><a href="selector.html">Klima Seçici</a><a href="regions.html">Hizmet Bölgeleri</a><a href="blog.html">Rehber</a><a href="contact.html">İletişim</a></div><div class="header-tools"><a href="favorites.html" title="Favoriler">♡</a><a href="compare.html" title="Karşılaştır">⇄</a><a class="ghost" href="checkout.html">Sepet <span class="cart-count" data-cart-count>0</span></a></div></nav></header>`}
function renderFooter(){const root=document.querySelector('[data-site-footer]');if(!root)return;root.innerHTML=`<footer class="store-footer"><div class="footer-grid"><div><a class="brand light" href="index.html">EGE TEKNİK<small>KLİMA & TEKNOLOJİ</small></a><p>${business.address}</p><p><a href="${business.phoneHref}">${business.phone}</a></p></div><div><b>Ürünler</b><a href="catalog.html">GREE Klimalar</a><a href="catalog.html?category=Yedek%20Parça">Yedek Parça</a><a href="second-hand.html">İkinci El & Outlet</a></div><div><b>Destek</b><a href="services.html">Montaj ve Servis</a><a href="selector.html">Klima Seçici</a><a href="contact.html">Servis Talebi</a></div><div><b>Kurumsal</b><a href="contact.html">İletişim</a><a href="policies.html">Satış ve İade Koşulları</a><a href="policies.html#privacy">KVKK ve Gizlilik</a></div></div><div class="footer-bottom">© 2026 Ege Teknik • Kuşadası / Aydın</div></footer>`}
function applyCatalogQuery(){const params=new URLSearchParams(location.search),cat=params.get('category'),btu=params.get('btu'),q=params.get('q');if(q&&document.querySelector('#catalog-search'))document.querySelector('#catalog-search').value=q;if(cat){const radio=[...document.querySelectorAll('[name=category]')].find(x=>x.value===cat||cat.includes(x.value));if(radio)radio.checked=true}if(btu){const chip=[...document.querySelectorAll('.chip')].find(x=>x.dataset.btu&&btu.includes(x.dataset.btu.replace('.','')));if(chip){document.querySelectorAll('.chip').forEach(x=>x.classList.remove('active'));chip.classList.add('active')}}renderCatalog()}
/* The id comes from the URL and is only a lookup key. An unknown or missing id says so -
   it never silently shows a different product - and nothing here is taken from a
   bundled price list: every figure is the served catalog's. */
function renderProductPage(){const root=document.querySelector('[data-product-page]');if(!root)return;
  if(!catalogAuthoritative()){root.innerHTML=`<div class="empty">${catalogNotice()}</div>`;return}
  const id=new URLSearchParams(location.search).get('id'),p=id?getProducts().find(x=>x.id===id):null;
  if(!p){root.innerHTML='<nav class="breadcrumbs"><a href="index.html">Ana Sayfa</a><span>›</span><a href="catalog.html">Ürünler</a></nav><div class="empty">Ürün bulunamadı. Ürün kaldırılmış veya bağlantı hatalı olabilir. <a href="catalog.html">Kataloğa dönün.</a></div>';document.title='Ürün bulunamadı | Ege Teknik';return}
  const name=esc(p.name),pid=esc(p.id),kesif=`contact.html?subject=kesif&product=${encodeURIComponent(p.id)}`,row=(label,value,tag)=>value?`<div><${tag==='key'?'small':'span'}>${label}</${tag==='key'?'small':'span'}><b>${esc(value)}</b></div>`:'';
  root.innerHTML=`<nav class="breadcrumbs"><a href="index.html">Ana Sayfa</a><span>›</span><a href="catalog.html">Ürünler</a><span>›</span><span>${name}</span></nav><section class="detail-grid"><div class="detail-visual"><span class="badge ${p.sale?'':'quote'}">${p.sale?'ONLINE SATIŞ':'TEKLİF / KEŞİF'}</span>${productImageMarkup(p,'detail-image','')||'<div class="unit large"></div><p>Temsili görünüm • Ürün görseli henüz eklenmedi</p>'}</div><div class="detail-buy"><div class="eyebrow">${esc([p.category,p.series].filter(Boolean).join(' / '))}</div><h1>${name}</h1>${p.sku?`<div class="sku">Model kodu: ${esc(p.sku)}</div>`:''}<div class="key-grid">${row('Kapasite',p.capacity,'key')}${row('Enerji',p.energy,'key')}${row('Wi-Fi',p.wifi,'key')}<div><small>Teslimat</small><b>${p.sale?'Sipariş sonrası teyit edilir':'Keşif sonrası'}</b></div></div><div class="price big">${p.sale?money(p.price):'Proje bazlı fiyat'}<small>${p.sale?(p.stock>0?`Güncel stok: ${esc(p.stock)}`:'Stokta yok'):'Ege Bölgesi için teklif alın'}</small></div><div class="buy-actions">${p.sale&&p.stock>0?`<button type="button" class="primary" data-action="add-cart" data-id="${pid}">Sepete Ekle</button>`:''}<button type="button" class="ghost" data-action="quote" data-id="${pid}">WhatsApp’tan Sor</button><a class="ghost" href="${kesif}">Keşif Talep Et</a></div><div class="trust-list"><span>✓ Montaj kapsamı sipariş öncesi teyit edilir</span><span>✓ Ege Bölgesi servis desteği</span></div></div></section><section class="content-section"><h2>Ürün özellikleri</h2><p>${name}, konut ve ticari kullanım ihtiyaçlarına göre değerlendirilen inverter iklimlendirme çözümüdür. Doğru kapasite seçimi için oda alanı, cephe, yalıtım ve bölgesel sıcaklık birlikte dikkate alınmalıdır.</p><div class="spec-table">${row('Ürün tipi',p.category)}${row('Nominal kapasite',p.capacity)}${row('Enerji sınıfı',p.energy)}${row('Kablosuz kontrol',p.wifi)}<div><span>Satış yöntemi</span><b>${p.sale?'Online / mağaza':'Teklif ve keşif'}</b></div></div></section><section class="content-section"><h2>Montaj ve keşif</h2><p>Standart montaj kapsamı; keşif sonucu, borulama mesafesi, elektrik altyapısı, dış ünite erişimi ve gerekli ek ekipmanlara göre kesinleştirilir. Kırım, karot, iskele, vinç ve elektrik tesisatı gibi ilave işler ayrıca değerlendirilir.</p><a class="primary inline" href="${kesif}">Bilgi ve keşif iste</a></section><section class="content-section"><h2>Teknik dokümanlar</h2><p>Ürün bilgi formu, enerji etiketi ve kullanım kılavuzu henüz sitede yayınlanmadı; ihtiyaç halinde <a href="contact.html">bizimle iletişime geçin</a>.</p></section>`;document.title=p.name+' | Ege Teknik Kuşadası'}
const regions=[['aydin','Aydın'],['izmir','İzmir'],['mugla','Muğla'],['manisa','Manisa'],['denizli','Denizli'],['usak','Uşak'],['afyonkarahisar','Afyonkarahisar'],['kutahya','Kütahya']];
function renderRegions(){const root=document.querySelector('[data-regions]');if(root)root.innerHTML=regions.map(([s,n])=>`<a class="region-card" href="region.html?city=${s}"><span>Hizmet bölgesi</span><b>${n}</b><small>Satış • keşif • montaj • servis</small></a>`).join('')}
function renderRegionPage(){const root=document.querySelector('[data-region-page]');if(!root)return;const slug=new URLSearchParams(location.search).get('city')||'aydin';/* The URL value is only ever used as a lookup key: an unknown city renders the generic page and is never echoed into the markup. */const region=regions.find(x=>x[0]===slug),name=region?.[1]||'Ege Bölgesi';root.innerHTML=`<div class="eyebrow">Ege Teknik hizmet bölgesi</div><h1>${name} Klima Satış, Montaj ve Servis</h1><p class="lead">Kuşadası merkezli Ege Teknik; ${name} genelinde GREE klima satışı, keşif, montaj, bakım, onarım ve yedek parça taleplerini planlı servis organizasyonuyla karşılar.</p><div class="service-grid"><article><b>Klima satışı</b><p>İhtiyaca uygun BTU ve seri seçimi, online sipariş veya teklif.</p></article><article><b>Keşif ve montaj</b><p>Mekân koşulları, borulama ve dış ünite konumunun değerlendirilmesi.</p></article><article><b>Bakım ve onarım</b><p>Periyodik bakım, arıza kontrolü ve parça temini.</p></article></div><div class="region-cta"><div><h2>${name} için servis veya keşif isteyin</h2><p>Adresinizi ve ihtiyacınızı iletin; uygun hizmet planı için sizi arayalım.</p></div><a class="primary" href="contact.html${region?`?city=${region[0]}`:''}">Talep oluştur</a></div>`;document.title=name+' Klima Servisi ve Satış | Ege Teknik'}
const articles=[['klima-btu-hesaplama','Klima BTU Hesaplama Rehberi','Doğru kapasiteyi alan, cephe, yalıtım ve bölgesel sıcaklıkla birlikte hesaplayın.'],['9000-btu-kac-metrekare','9.000 BTU Klima Kaç m² İçin Uygundur?','Küçük oda ve çalışma alanlarında kapasiteyi doğru değerlendirin.'],['12000-btu-kac-metrekare','12.000 BTU Klima Kaç m² İçin Uygundur?','Salon ve orta büyüklükte odalar için seçim ölçütleri.'],['18000-btu-kac-metrekare','18.000 BTU Klima Kaç m² İçin Uygundur?','Geniş ve güneş alan mekânlarda kapasite seçimi.'],['24000-btu-nerede-kullanilir','24.000 BTU Klima Nerelerde Kullanılır?','Büyük salonlar ve ticari alanlar için temel değerlendirme.'],['gree-serileri-karsilastirma','GREE Airy, Fairy, Pular ve Aphro Karşılaştırması','Bütçe, sessizlik, enerji verimliliği ve akıllı özelliklere göre seri seçimi.'],['inverter-klima-nedir','Inverter Klima Nedir?','Değişken kompresör çalışma mantığı ve enerji tüketimine etkisi.'],['klima-elektrik-tuketimi','Klima Ne Kadar Elektrik Harcar?','BTU, enerji sınıfı ve kullanım alışkanlıklarının faturaya etkisi.'],['enerji-sinifi-farki','A++ ve A+++ Arasındaki Fark','Enerji etiketi, SEER ve SCOP değerlerini doğru okuyun.'],['klima-neden-sogutmaz','Klima Neden Soğutmaz?','Filtre, gaz, sensör ve dış ünite kaynaklı yaygın sorunlar.'],['klima-neden-su-akitir','Klima Neden Su Akıtır?','Drenaj, filtre ve montaj eğiminden kaynaklanan olasılıklar.'],['klima-bakimi-ne-zaman','Klima Bakımı Ne Zaman Yapılmalı?','Kullanım yoğunluğuna göre filtre ve teknik bakım aralıkları.'],['ikinci-el-klima-alinir-mi','İkinci El Klima Alınır mı?','Kompresör, gaz basıncı, hijyen ve montaj geçmişinde kontrol edilmesi gerekenler.'],['kusadasi-klima-secimi','Kuşadası İkliminde Klima Seçimi','Nem, yazlık kullanım ve güneş alan cepheler için kapasite önerileri.'],['yazlik-ev-klima-secimi','Yazlık Evler İçin Klima Seçimi','Sezonluk kullanım, nem ve uzun süre kapalı kalma koşulları.']];
function renderBlog(){const root=document.querySelector('[data-blog]');if(root)root.innerHTML=articles.map(([s,t,d])=>`<article class="article-card"><span>Klima Rehberi</span><h2><a href="article.html?slug=${s}">${t}</a></h2><p>${d}</p><a href="article.html?slug=${s}">Yazıyı oku →</a></article>`).join('')}
function renderArticle(){const root=document.querySelector('[data-article]');if(!root)return;const slug=new URLSearchParams(location.search).get('slug')||articles[0][0];const a=articles.find(x=>x[0]===slug)||articles[0];root.innerHTML=`<div class="eyebrow">Ege Teknik Klima Rehberi</div><h1>${a[1]}</h1><p class="article-meta">Teknik kontrol: Ege Teknik • Güncelleme: Eylül 2026 • 6 dk okuma</p><p class="lead">${a[2]}</p><div class="answer-box"><b>Kısa cevap</b><p>Klima seçimi veya arıza değerlendirmesi tek bir değere göre yapılmamalıdır. Mekânın fiziksel koşulları, ürün kapasitesi ve doğru montaj birlikte ele alınmalıdır.</p></div><h2>Doğru değerlendirme nasıl yapılır?</h2><p>Alan ölçüsü başlangıç noktasıdır; tavan yüksekliği, güneş alan cephe, pencere yüzeyi, yalıtım, kişi sayısı ve bulunduğunuz şehir sonucu değiştirir. Ege Bölgesi’nde kıyı nemi ve uzun soğutma sezonu da seçimde önemlidir.</p><h2>Karar verirken kontrol listesi</h2><ul><li>Odanın net alanını ve tavan yüksekliğini ölçün.</li><li>Güneş alan cepheyi ve pencere büyüklüğünü değerlendirin.</li><li>Elektrik altyapısı ile dış ünite konumunu kontrol edin.</li><li>Yalnız fiyatı değil enerji verimliliği ve servis erişimini karşılaştırın.</li><li>Kesinleşmeyen durumlarda yerinde keşif isteyin.</li></ul><h2>Sık sorulan sorular</h2><details><summary>Online hesaplama kesin sonuç verir mi?</summary><p>Hayır. Ön öneri sağlar; zor mekânlarda yerinde keşif gerekir.</p></details><details><summary>Ege Teknik hangi bölgelere hizmet verir?</summary><p>Kuşadası merkezli olarak tüm Ege Bölgesi için satış ve planlı servis talepleri alınır.</p></details><div class="region-cta"><div><h2>Uzman desteği alın</h2><p>Ürün seçimi, keşif veya servis için Ege Teknik’e ulaşın.</p></div><a class="primary" href="contact.html">Bize ulaşın</a></div>`;document.title=a[1]+' | Ege Teknik'}
function renderContactForm(){const f=document.querySelector('[data-contact-form]');if(!f)return;const q=new URLSearchParams(location.search);f.querySelector('[name=subject]').value=q.get('subject')||'urun';f.querySelector('[name=city]').value=q.get('city')||'';f.addEventListener('submit',async e=>{e.preventDefault();const d=new FormData(f);const button=f.querySelector('button');button.disabled=true;button.textContent='Talebiniz kaydediliyor…';const payload={type:d.get('subject'),name:d.get('name'),phone:d.get('phone'),city:d.get('city'),message:d.get('message')},attemptKey=sessionStorage.getItem('ege-service-attempt')||crypto.randomUUID();sessionStorage.setItem('ege-service-attempt',attemptKey);try{const response=await fetch('/api/service-requests',{method:'POST',headers:{'content-type':'application/json','idempotency-key':attemptKey},body:JSON.stringify(payload)});const result=await response.json();if(!response.ok)throw new Error(result.error||'Talep kaydedilemedi');sessionStorage.removeItem('ege-service-attempt');const msg=`Ege Teknik web talebi (${result.requestNumber})\nKonu: ${d.get('subject')}\nAd: ${d.get('name')}\nTelefon: ${d.get('phone')}\nŞehir: ${d.get('city')}\nMesaj: ${d.get('message')}`;button.textContent='Kaydedildi — WhatsApp açılıyor';setTimeout(()=>{location.href=business.wa+'?text='+encodeURIComponent(msg)},600)}catch(error){button.disabled=false;button.textContent='Tekrar deneyin';alert(error.message||'Talep kaydedilemedi. Lütfen telefonla ulaşın.')}})}
function calculateBtu(){const area=Number(document.querySelector('#sel-area')?.value||20),sun=Number(document.querySelector('#sel-sun')?.value||1),ins=Number(document.querySelector('#sel-ins')?.value||1);const raw=area*500*sun*ins;const btu=raw<=9000?9000:raw<=12000?12000:raw<=18000?18000:24000;const out=document.querySelector('[data-selector-result]');out.innerHTML=`<span>Ön kapasite önerisi</span><b>${new Intl.NumberFormat('tr-TR').format(btu)} BTU</b><p>Bu sonuç ön değerlendirmedir. Montaj yeri ve teknik koşullar için keşif önerilir.</p><a class="primary inline" href="catalog.html?btu=${btu}">Uygun modelleri gör</a>`;out.classList.add('show')}
/* Homepage featured cards: the static markup only names a product by id
   (data-featured-product); every price, stock and name shown comes from the served
   catalog and is written with textContent. A product that is not in the catalog shows
   that it is not listed - never a remembered price. */
function renderFeaturedProducts(){document.querySelectorAll('[data-featured-product]').forEach(card=>{const set=(field,text)=>card.querySelectorAll(`[data-product-field="${field}"]`).forEach(el=>{el.textContent=text});
  if(catalogState==='loading'){set('price','Fiyat yükleniyor…');set('stock','');return}
  const p=catalogAuthoritative()?getProducts().find(x=>x.id===card.dataset.featuredProduct):null;
  if(!p){set('price',catalogState==='unavailable'?'Fiyat bilgisi alınamadı':'Şu anda listelenmiyor');set('stock','');return}
  set('name',p.name);set('capacity',p.capacity||'—');set('sku',p.sku||'—');set('price',p.sale?money(p.price):'Fiyat için teklif alın');set('stock',p.sale?(p.stock>0?`Stok: ${p.stock}`:'Stokta yok'):'')})}
function renderFavorites(){const root=document.querySelector('[data-favorites]');if(!root)return;if(!catalogAuthoritative()){root.innerHTML=`<div class="empty">${catalogNotice()}</div>`;return}const ps=getProducts().filter(p=>getFavorites().includes(p.id));root.innerHTML=ps.length?ps.map(productCard).join(''):'<div class="empty">Henüz favori ürününüz yok. <a href="catalog.html">Ürünleri inceleyin.</a></div>'}
function renderCompare(){const root=document.querySelector('[data-compare]');if(!root)return;if(!catalogAuthoritative()){root.innerHTML=`<div class="empty">${catalogNotice()}</div>`;return}const ps=getProducts().filter(p=>getCompare().includes(p.id));if(!ps.length){root.innerHTML='<div class="empty">Karşılaştırma listeniz boş. <a href="catalog.html">Katalogdan ürün ekleyin.</a></div>';return}const cells=f=>ps.map(p=>`<span>${esc(f(p)||'—')}</span>`).join('');root.innerHTML=`<div class="compare-grid"><b>Özellik</b>${ps.map(p=>`<b>${esc(p.name)}</b>`).join('')}<span>Kapasite</span>${cells(p=>p.capacity)}<span>Enerji sınıfı</span>${cells(p=>p.energy)}<span>Wi-Fi</span>${cells(p=>p.wifi)}<span>Fiyat</span>${cells(p=>p.sale?money(p.price):'Teklif')}<span>İşlem</span>${ps.map(p=>`<span><a href="product.html?id=${encodeURIComponent(p.id)}">İncele</a><br><button type="button" class="ghost" data-action="toggle-compare" data-id="${esc(p.id)}">Çıkar</button></span>`).join('')}</div>`;root.style.setProperty('--cols',ps.length+1)}
/* Only absolute http(s) URLs or site-root paths are used as image sources. */
function safeImageSrc(u){const s=String(u??'').trim();if(s.startsWith('/')&&!s.startsWith('//'))return s;try{const url=new URL(s);return /^https?:$/.test(url.protocol)?url.href:''}catch{return ''}}
const imageTag=(u,alt,extra='')=>{const src=safeImageSrc(u);return src?`<img src="${esc(src)}" alt="${esc(alt)}"${extra}>`:''};
/* Admin-authored post content is plain text: blank lines separate paragraphs, single
   newlines become <br>. It is escaped first, so no markup in it is ever interpreted. */
const plainTextParagraphs=t=>String(t??'').split(/\n{2,}/).map(x=>`<p>${esc(x).replace(/\n/g,'<br>')}</p>`).join('');
function renderManagedBlog(posts){const root=document.querySelector('[data-blog]');if(root)root.innerHTML=posts.map(p=>{const href=`article.html?slug=${encodeURIComponent(p.slug)}`;return `<article class="article-card">${imageTag(p.imageUrl,p.title,' loading="lazy"')}<span>Ege Teknik Rehberi</span><h2><a href="${href}">${esc(p.title)}</a></h2><p>${esc(p.excerpt)}</p><a href="${href}">Yazıyı oku →</a></article>`}).join('');const article=document.querySelector('[data-article]'),slug=new URLSearchParams(location.search).get('slug'),post=posts.find(p=>p.slug===slug);if(article&&post){article.innerHTML=`<div class="eyebrow">Ege Teknik Klima Rehberi</div><h1>${esc(post.title)}</h1>${imageTag(post.imageUrl,post.title,' class="article-cover"')}<p class="lead">${esc(post.excerpt)}</p><div class="managed-content">${plainTextParagraphs(post.content)}</div>`;document.title=post.title+' | Ege Teknik'}}
const secondHandHref=p=>`contact.html?subject=ikinci-el&product=${encodeURIComponent(p.slug)}`;
function renderManagedSecondHand(products){const root=document.querySelector('.second-grid');if(root&&products.length)document.querySelector('[data-second-hand-empty]')?.remove();if(root&&products.length)root.innerHTML=products.map(p=>`<article class="second-card">${imageTag(p.imageUrl,p.name,' loading="lazy"')}<small>${esc(p.category)} • ${esc(p.condition)}</small><h2>${esc(p.name)}</h2><p>${esc(p.description)}</p><p><b>${money(p.price)}</b> • Stok: ${esc(p.stock)}</p><p>${esc(p.testNotes)}</p><button type="button" class="ghost" data-action="navigate" data-href="${esc(secondHandHref(p))}">Bilgi al</button></article>`).join('')}
/* Homepage outlet highlights: only real published second-hand stock, or an honest empty/unavailable state. */
function renderSecondHandHighlights(products){const root=document.querySelector('[data-home-second-hand]');if(!root)return;
  if(products===null){root.innerHTML='<p class="md:col-span-2 text-center text-surface-container-high font-body-md text-body-md py-space-lg">İkinci el stok bilgisi şu anda alınamıyor. Güncel durum için ikinci el sayfasını ziyaret edin.</p>';return}
  if(!products.length){root.innerHTML='<p class="md:col-span-2 text-center text-surface-container-high font-body-md text-body-md py-space-lg">Şu anda yayında ikinci el ürün bulunmuyor. Yeni stok geldiğinde haber almak için <a class="text-secondary-fixed-dim underline" href="contact.html?subject=ikinci-el">talep bırakın</a>.</p>';return}
  root.innerHTML=products.slice(0,2).map(p=>`<article class="bg-[#1b2623] rounded-2xl p-space-lg text-surface-container-lowest flex flex-col gap-space-sm">${imageTag(p.imageUrl,p.name,' class="w-full aspect-[16/10] object-cover rounded-xl" loading="lazy"')}<span class="font-label-sm text-label-sm uppercase tracking-wider text-secondary-fixed-dim">${esc(p.category)} • ${esc(p.condition)}</span><h3 class="font-headline-sm text-headline-sm font-bold">${esc(p.name)}</h3><p class="font-body-sm text-body-sm text-surface-container-high">${esc(p.description)}</p><p class="font-metric-display text-metric-display">${money(p.price)}</p><a class="self-start px-4 py-2 rounded-lg bg-secondary text-surface-container-lowest font-label-md text-label-md font-bold" href="${esc(secondHandHref(p))}">Bilgi al</a></article>`).join('')}
async function loadManagedContent(){const [b,s]=await Promise.allSettled([fetch('/api/blog'),fetch('/api/second-hand')]);
  try{if(b.status==='fulfilled'&&b.value.ok){const {posts}=await b.value.json();if(Array.isArray(posts)&&posts.length)renderManagedBlog(posts)}}catch{console.info('Rehber içeriği başlangıç verisiyle gösteriliyor.')}
  let secondHand=null;try{if(s.status==='fulfilled'&&s.value.ok){const {products}=await s.value.json();secondHand=Array.isArray(products)?products:[]}}catch{secondHand=null}
  if(secondHand)renderManagedSecondHand(secondHand);renderSecondHandHighlights(secondHand)}
document.addEventListener('DOMContentLoaded',()=>{renderHeader();renderFooter();updateCartCount();updateFavoritesCount();renderFeaturedProducts();applyCatalogQuery();renderProductPage();renderRegions();renderRegionPage();renderBlog();renderArticle();renderContactForm();renderFavorites();renderCompare()});
document.addEventListener('DOMContentLoaded',()=>{void loadManagedContent()});
